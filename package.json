{
  "name": "sign",
  "version": "0.1.0",
  "private": true,
  "description": "線上補簽同意書 web app",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js",
    "package:windows": "powershell -ExecutionPolicy Bypass -File scripts/build-windows-exe.ps1"
  },
  "engines": {
    "node": ">=20"
  }
}
